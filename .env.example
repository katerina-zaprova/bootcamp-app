# Copy to .env for local overrides (never commit .env)

# Server
NODE_ENV=development
PORT=3001

# Database path — defaults to server/data.db
# On Render paid tier, mount a disk at /var/data and set:
# DB_PATH=/var/data/verity.db
